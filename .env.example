# URL base da API Django. Deixe vazio em desenvolvimento para usar o proxy do Vite (/api -> http://127.0.0.1:8000).
# Exemplo chamando o Django direto (sem proxy):
# VITE_API_URL=http://127.0.0.1:8000
VITE_API_URL=
